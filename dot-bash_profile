. ~/.bashrc
export PATH=/opt/local/bin:/opt/local/sbin:$PATH:/usr/local/ada-4.3/bin

export CLOJURE_EXT=~/opt/lisp/ext

if [ -f /usr/local/etc/bash_completion ]; then
  . /usr/local/etc/bash_completion
fi

lesskey
